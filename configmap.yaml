apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  # Ejemplo de variables de entorno
  DATABASE_HOST: localhost
  DATABASE_PORT: "5432"
  API_URL: https://api.example.com
  LOG_LEVEL: info