struct Point {
    var x: Double
    var y: Double
}

// Пример использования
let point1 = Point(x: 3.5, y: 7.2)
print("Точка 1: x = \(point1.x), y = \(point1.y)")

let origin = Point(x: 0.0, y: 0.0)
print("Начало координат: x = \(origin.x), y = \(origin.y)")