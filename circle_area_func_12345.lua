-- Function to calculate the area of a circle
function area_of_circle(radius)
    local pi = 3.14159
    return pi * radius * radius
end

-- Example usage (optional)
-- local r = 5
-- print("Area of circle with radius " .. r .. " is " .. area_of_circle(r))