// Структура для хранения информации о точке
const Point = struct {
    x: f32,
    y: f32,
};

// Пример использования
pub fn main() void {
    const my_point = Point{ .x = 3.14, .y = 2.71 };
    // В реальном приложении здесь могла бы быть логика для вывода или использования my_point
}