# Ruby script to traverse an array using a block and print elements

# Define an array of fruits
fruits = ['apple', 'banana', 'cherry', 'date']

# Use the .each method with a block to iterate over each element
fruits.each do |fruit|
  # Print each fruit to the console
  puts fruit
end