pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add your build commands here
                // sh 'make build'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing...'
                // Add your test commands here
                // sh 'make test'
            }
        }
    }

    post {
        always {
            echo 'Pipeline completed'
        }
    }
}
