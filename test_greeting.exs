#!/usr/bin/env elixir

# Load the compiled module
Code.load_file("YakfVvWJav.ex")

# Test the greeting functions
IO.puts(Greeting.hello("World"))
IO.puts(Greeting.hello("Elixir"))
IO.puts(Greeting.general())