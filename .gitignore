```
# GraphQL schema files are source code - don't ignore them
# schema.graphql and f870896a5e999b99.graphql should be versioned

# No build artifacts, dependencies, or temp files detected in changes
# Only GraphQL schema files were added, which are source files
```