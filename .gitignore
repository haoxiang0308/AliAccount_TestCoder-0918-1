```
# Kubernetes manifests
*.yaml
*.yml

# If this is a Kubernetes project, we might want to ignore
# common Kubernetes artifacts, but since we only see a deployment file
# and no other artifacts, we'll keep it minimal
```