```
# SQL query files (if they are temporary/generated)
*.sql

# If query files are meant to be temporary, you can be more specific
# For example, if they follow a pattern like query_*.sql:
query_*.sql
```