```
# Kubernetes/OpenShift deployment files
*.yaml
*.yml

# If this is a Kubernetes/Openshift project, you might want to ignore other common files
# but based on the single YAML file added, this is the most relevant ignore
```