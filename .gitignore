(db_connect.php is a source code file, and there are no build artifacts, dependencies, or temp files in the changes - only a PHP source file was added, so no ignores are needed)

(empty - output nothing)