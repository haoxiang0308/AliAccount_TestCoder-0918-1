```toml
# Configuration files that might contain sensitive data
*.toml
!app_config.toml
!ob4l1enocs.toml
```

Wait, I need to reconsider. The .toml files are explicitly added and appear to be configuration files that should be tracked, not ignored. Since the changes only include .toml files (which are source/config files), no files need to be ignored.

(empty)