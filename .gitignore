```
# XML data files (if they are generated/temporary)
xml_data/*.xml

# If xml_data contains only manually created files, this line can be removed
# Currently including it since XML files might be data artifacts
```