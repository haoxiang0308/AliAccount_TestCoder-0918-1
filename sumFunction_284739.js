// Функция для сложения двух чисел
function add(a, b) {
  return a + b;
}

// Пример использования
console.log(add(2, 3)); // Выведет 5
console.log(add(-1, 5)); // Выведет 4

// Экспорт функции для возможного использования в других файлах
module.exports = add;