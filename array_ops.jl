# Basic Array Operations in Julia

# Define an example array
arr = [1, 2, 3, 4, 5]

# Calculate the sum
arr_sum = sum(arr)

# Calculate the mean
arr_mean = mean(arr)

println("Array: ", arr)
println("Sum: ", arr_sum)
println("Mean: ", arr_mean)
