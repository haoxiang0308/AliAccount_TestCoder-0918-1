# PowerShell Script to Save Processes

This repository contains a PowerShell script that retrieves a list of processes and saves them to a file with a random name.

## Usage

To run the script, execute the following command in PowerShell:

```powershell
.\get_processes.ps1
```

This will create a file with a random name in the format `processes_XXXX.txt` containing the list of processes.