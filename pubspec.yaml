name: network_data_saver
description: A sample command-line application that fetches network data and saves it to a randomly named .dart file.

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.1.0

dev_dependencies:
  lints: ^3.0.0